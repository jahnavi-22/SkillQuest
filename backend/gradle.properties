# Pin this project's Gradle build to Java 17 without changing the system default JDK.
# Your global `java -version` (25) is untouched; this only affects the SkillQuest backend.
org.gradle.java.home=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
